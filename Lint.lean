import Sequencelib
#lint+ only checkType checkUnivs defLemma deprecatedNoSince dupNamespace
  explicitVarsOfIff impossibleInstance nonClassInstance simpComm simpNF
  simpVarHead structureInType synTaut unusedArguments unusedHavesSuffices in Sequencelib

def main : IO Unit := do
  return ()
